#include <stdio.h>

/**
 * main - Prints all numbers base 10 starting from 0.
 *
 * Return: Always 0.
 */
int main(void)
{
	int nu;

	for (nu = 0; nu < 10; nu++)
		printf("%d", nu);

	printf("\n");

	return (0);
}
