#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: An input string.
 *
 * Return: An integer
 */

int _strlen(char *s)
{
	int n = strlen(s);

	return (n);

}

