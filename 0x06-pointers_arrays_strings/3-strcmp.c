#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: An input character
 * @s2: An input character
 * 
 * Return: differnce between strings
 */

int _strcmp(char *s1, char *s2)
{
	int r;

	r = strcmp(s1, s2);
	return (r);
}
